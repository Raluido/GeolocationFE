import { LoadShapesDirective } from './load-shapes.directive';

describe('LoadShapesDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadShapesDirective();
    expect(directive).toBeTruthy();
  });
});
